<?php

/**
 * PHP SDK for weibo.com (using OAuth2)
 * 
 * @author Elmer Zhang <freeboy6716@gmail.com>
 */
/**
 * If the class OAuthException has not been declared, extend the Exception class.
 * This is to allow OAuth without the PECL OAuth library
 * 
 * @ignore
 */
if (!class_exists('OAuthException')) {

    class OAuthException extends Exception {
        // pass
    }

}

/**
 * ???? OAuth ???(OAuth2)
 *
 * ????????????????????{@link http://open.weibo.com/wiki/Oauth2}
 *
 * @package sae
 * @author Elmer Zhang
 * @version 1.0
 */
class SaeTOAuthV2 {

    /**
     * @ignore
     */
    public $client_id;

    /**
     * @ignore
     */
    public $client_secret;

    /**
     * @ignore
     */
    public $access_token;

    /**
     * @ignore
     */
    public $refresh_token;

    /**
     * Contains the last HTTP status code returned. 
     *
     * @ignore
     */
    public $http_code;

    /**
     * Contains the last API call.
     *
     * @ignore
     */
    public $url;

    /**
     * Set up the API root URL.
     *
     * @ignore
     */
    public $host = "https://api.weibo.com/2/";

    /**
     * Set timeout default.
     *
     * @ignore
     */
    public $timeout = 30;

    /**
     * Set connect timeout.
     *
     * @ignore
     */
    public $connecttimeout = 30;

    /**
     * Verify SSL Cert.
     *
     * @ignore
     */
    public $ssl_verifypeer = FALSE;

    /**
     * Respons format.
     *
     * @ignore
     */
    public $format = 'json';

    /**
     * Decode returned json data.
     *
     * @ignore
     */
    public $decode_json = TRUE;

    /**
     * Contains the last HTTP headers returned.
     *
     * @ignore
     */
    public $http_info;

    /**
     * Set the useragnet.
     *
     * @ignore
     */
    public $useragent = 'Sae T OAuth2 v0.1';

    /**
     * print the debug info
     *
     * @ignore
     */
    public $debug = FALSE;

    /**
     * boundary of multipart
     * @ignore
     */
    public static $boundary = '';

    /**
     * Set API URLS
     */

    /**
     * @ignore
     */
    function accessTokenURL() {
        return 'https://api.weibo.com/oauth2/access_token';
    }

    /**
     * @ignore
     */
    function authorizeURL() {
        return 'https://api.weibo.com/oauth2/authorize';
    }

    /**
     * @ignore
     */
    function tokenInfoURL() {
        return 'https://api.weibo.com/oauth2/get_token_info';
    }

    /**
     * @ignore
     */
    function revokeOAuthURL() {
        return 'https://api.weibo.com/oauth2/revokeoauth2';
    }

    /**
     * construct WeiboOAuth object
     */
    function __construct($client_id, $client_secret, $access_token = NULL, $refresh_token = NULL) {
        $this->client_id = $client_id;
        $this->client_secret = $client_secret;
        $this->access_token = $access_token;
        $this->refresh_token = $refresh_token;
    }

    /**
     * authorize??
     *
     * ??API?{@link http://open.weibo.com/wiki/Oauth2/authorize Oauth2/authorize}
     *
     * @param string $url ????????,????????????,????????canvas page???
     * @param string $response_type ?????? code ?token ????code
     * @param string $state ?????????????????,??Query Parameter??????
     * @param string $display ?????? ????: 
     *  - default		??????		
     *  - mobile		??html5???		
     *  - popup			?????		
     *  - wap1.2		wap1.2??		
     *  - wap2.0		wap2.0??		
     *  - js			js-sdk ?? ?????????????js-sdk????		
     *  - apponweibo	??????,??????display??,??response_type?token?,?????display.???????access_token?????js?????????
     * @return array
     */
    function getAuthorizeURL($url, $response_type = 'code', $state = NULL, $display = NULL) {
        $params = array();
        $params['client_id'] = $this->client_id;
        $params['redirect_uri'] = $url;
        $params['response_type'] = $response_type;
        $params['state'] = $state;
        $params['display'] = $display;
        return $this->authorizeURL() . "?" . http_build_query($params);
    }

    function getTokenInfo($access_token) {
        $params = array();
        $params['access_token'] = $access_token;
        $response = $this->oAuthRequest($this->tokenInfoURL(), 'POST', $params);
        $token_info = json_decode($response, true);
        if (is_array($token_info) && !isset($token['error'])) {
            $info = $token_info;
        } else {
            throw new OAuthException("get token info failed." . $token['error']);
        }
        return $info;
    }

    function revokeAuth($access_token) {
        $params = array();
        $params['access_token'] = $access_token;
        $response = $this->oAuthRequest($this->revokeOAuthURL(), 'POST', $params);
        $revoke = json_decode($response, true);
        if (is_array($revoke) && !isset($revoke['error'])) {
            $revoke_info = $revoke;
        } else {
            throw new OAuthException("revoke auth failed." . $revoke['error']);
        }
        return $revoke_info;
    }

    /**
     * access_token??
     *
     * ??API?{@link http://open.weibo.com/wiki/OAuth2/access_token OAuth2/access_token}
     *
     * @param string $type ?????,???:code, password, token
     * @param array $keys ?????
     *  - ?$type?code?? array('code'=>..., 'redirect_uri'=>...)
     *  - ?$type?password?? array('username'=>..., 'password'=>...)
     *  - ?$type?token?? array('refresh_token'=>...)
     * @return array
     */
    function getAccessToken($type = 'code', $keys) {
        $params = array();
        $params['client_id'] = $this->client_id;
        $params['client_secret'] = $this->client_secret;
        if ($type === 'token') {
            $params['grant_type'] = 'refresh_token';
            $params['refresh_token'] = $keys['refresh_token'];
        } elseif ($type === 'code') {
            $params['grant_type'] = 'authorization_code';
            $params['code'] = $keys['code'];
            $params['redirect_uri'] = $keys['redirect_uri'];
        } elseif ($type === 'password') {
            $params['grant_type'] = 'password';
            $params['username'] = $keys['username'];
            $params['password'] = $keys['password'];
        } else {
            throw new OAuthException("wrong auth type");
        }

        $response = $this->oAuthRequest($this->accessTokenURL(), 'POST', $params);
        $token = json_decode($response, true);
        if (is_array($token) && !isset($token['error'])) {
            $this->access_token = $token['access_token'];
            //$this->refresh_token = $token['refresh_token'];
        } else {
            throw new OAuthException("get access token failed." . $token['error']);
        }
        return $token;
    }

    /**
     * ?? signed_request
     *
     * @param string $signed_request ???????iframe?????Canvas URL post???signed_request
     *
     * @return array
     */
    function parseSignedRequest($signed_request) {
        list($encoded_sig, $payload) = explode('.', $signed_request, 2);
        $sig = self::base64decode($encoded_sig);
        $data = json_decode(self::base64decode($payload), true);
        if (strtoupper($data['algorithm']) !== 'HMAC-SHA256')
            return '-1';
        $expected_sig = hash_hmac('sha256', $payload, $this->client_secret, true);
        return ($sig !== $expected_sig) ? '-2' : $data;
    }

    /**
     * @ignore
     */
    function base64decode($str) {
        return base64_decode(strtr($str . str_repeat('=', (4 - strlen($str) % 4)), '-_', '+/'));
    }

    /**
     * ??jssdk????????jssdk?????
     *
     * @return array ????array('access_token'=>'value', 'refresh_token'=>'value'); ????false
     */
    function getTokenFromJSSDK() {
        $key = "weibojs_" . $this->client_id;
        if (isset($_COOKIE[$key]) && $cookie = $_COOKIE[$key]) {
            parse_str($cookie, $token);
            if (isset($token['access_token']) && isset($token['refresh_token'])) {
                $this->access_token = $token['access_token'];
                $this->refresh_token = $token['refresh_token'];
                return $token;
            } else {
                return false;
            }
        } else {
            return false;
        }
    }

    /**
     * ??????access_token?refresh_token
     * ????Session?Cookie???token????Session/Cookie?????token???????
     *
     * @param array $arr ??access_token?secret_token???
     * @return array ????array('access_token'=>'value', 'refresh_token'=>'value'); ????false
     */
    function getTokenFromArray($arr) {
        if (isset($arr['access_token']) && $arr['access_token']) {
            $token = array();
            $this->access_token = $token['access_token'] = $arr['access_token'];
            if (isset($arr['refresh_token']) && $arr['refresh_token']) {
                $this->refresh_token = $token['refresh_token'] = $arr['refresh_token'];
            }

            return $token;
        } else {
            return false;
        }
    }

    /**
     * GET wrappwer for oAuthRequest.
     *
     * @return mixed
     */
    function get($url, $parameters = array()) {
        $response = $this->oAuthRequest($url, 'GET', $parameters);
        if ($this->format === 'json' && $this->decode_json) {
            return json_decode($response, true);
        }
        return $response;
    }

    /**
     * POST wreapper for oAuthRequest.
     *
     * @return mixed
     */
    function post($url, $parameters = array(), $multi = false) {
        $response = $this->oAuthRequest($url, 'POST', $parameters, $multi);
        if ($this->format === 'json' && $this->decode_json) {
            return json_decode($response, true);
        }
        return $response;
    }

    /**
     * DELTE wrapper for oAuthReqeust.
     *
     * @return mixed
     */
    function delete($url, $parameters = array()) {
        $response = $this->oAuthRequest($url, 'DELETE', $parameters);
        if ($this->format === 'json' && $this->decode_json) {
            return json_decode($response, true);
        }
        return $response;
    }

    /**
     * Format and sign an OAuth / API request
     *
     * @return string
     * @ignore
     */
    function oAuthRequest($url, $method, $parameters, $multi = false) {

        if (strrpos($url, 'http://') !== 0 && strrpos($url, 'https://') !== 0) {
            $url = "{$this->host}{$url}.{$this->format}";
        }

        switch ($method) {
            case 'GET':
                $url = $url . '?' . http_build_query($parameters);
                return $this->http($url, 'GET');
            default:
                $headers = array();
                if (!$multi && (is_array($parameters) || is_object($parameters))) {
                    $body = http_build_query($parameters);
                } else {
                    $body = self::build_http_query_multi($parameters);
                    $headers[] = "Content-Type: multipart/form-data; boundary=" . self::$boundary;
                }
                return $this->http($url, $method, $body, $headers);
        }
    }

    /**
     * Make an HTTP request
     *
     * @return string API results
     * @ignore
     */
    function http($url, $method, $postfields = NULL, $headers = array()) {
        $this->http_info = array();
        $ci = curl_init();
        /* Curl settings */
        curl_setopt($ci, CURLOPT_HTTP_VERSION, CURL_HTTP_VERSION_1_0);
        curl_setopt($ci, CURLOPT_USERAGENT, $this->useragent);
        curl_setopt($ci, CURLOPT_CONNECTTIMEOUT, $this->connecttimeout);
        curl_setopt($ci, CURLOPT_TIMEOUT, $this->timeout);
        curl_setopt($ci, CURLOPT_RETURNTRANSFER, TRUE);
        curl_setopt($ci, CURLOPT_ENCODING, "");
        curl_setopt($ci, CURLOPT_SSL_VERIFYPEER, $this->ssl_verifypeer);
        if (version_compare(phpversion(), '5.4.0', '<')) {
            curl_setopt($ci, CURLOPT_SSL_VERIFYHOST, 1);
        } else {
            curl_setopt($ci, CURLOPT_SSL_VERIFYHOST, 2);
        }
        curl_setopt($ci, CURLOPT_HEADERFUNCTION, array($this, 'getHeader'));
        curl_setopt($ci, CURLOPT_HEADER, FALSE);

        switch ($method) {
            case 'POST':
                curl_setopt($ci, CURLOPT_POST, TRUE);
                if (!empty($postfields)) {
                    curl_setopt($ci, CURLOPT_POSTFIELDS, $postfields);
                    $this->postdata = $postfields;
                }
                break;
            case 'DELETE':
                curl_setopt($ci, CURLOPT_CUSTOMREQUEST, 'DELETE');
                if (!empty($postfields)) {
                    $url = "{$url}?{$postfields}";
                }
        }

        if (isset($this->access_token) && $this->access_token)
            $headers[] = "Authorization: OAuth2 " . $this->access_token;

        if (!empty($this->remote_ip)) {
            if (defined('SAE_ACCESSKEY')) {
                $headers[] = "SaeRemoteIP: " . $this->remote_ip;
            } else {
                $headers[] = "API-RemoteIP: " . $this->remote_ip;
            }
        } else {
            if (!defined('SAE_ACCESSKEY')) {
                $headers[] = "API-RemoteIP: " . $_SERVER['REMOTE_ADDR'];
            }
        }
        curl_setopt($ci, CURLOPT_URL, $url);
        curl_setopt($ci, CURLOPT_HTTPHEADER, $headers);
        curl_setopt($ci, CURLINFO_HEADER_OUT, TRUE);

        $response = curl_exec($ci);
        $this->http_code = curl_getinfo($ci, CURLINFO_HTTP_CODE);
        $this->http_info = array_merge($this->http_info, curl_getinfo($ci));
        $this->url = $url;

        if ($this->debug) {
            echo "=====post data======\r\n";
            var_dump($postfields);

            echo "=====headers======\r\n";
            print_r($headers);

            echo '=====request info=====' . "\r\n";
            print_r(curl_getinfo($ci));

            echo '=====response=====' . "\r\n";
            print_r($response);
        }
        curl_close($ci);
        return $response;
    }

    /**
     * Get the header info to store.
     *
     * @return int
     * @ignore
     */
    function getHeader($ch, $header) {
        $i = strpos($header, ':');
        if (!empty($i)) {
            $key = str_replace('-', '_', strtolower(substr($header, 0, $i)));
            $value = trim(substr($header, $i + 2));
            $this->http_header[$key] = $value;
        }
        return strlen($header);
    }

    /**
     * @ignore
     */
    public static function build_http_query_multi($params) {
        if (!$params)
            return '';

        uksort($params, 'strcmp');

        $pairs = array();

        self::$boundary = $boundary = uniqid('------------------');
        $MPboundary = '--' . $boundary;
        $endMPboundary = $MPboundary . '--';
        $multipartbody = '';

        foreach ($params as $parameter => $value) {

            if (in_array($parameter, array('pic', 'image')) && $value{0} == '@') {
                $url = ltrim($value, '@');
                $content = file_get_contents($url);
                $array = explode('?', basename($url));
                $filename = $array[0];

                $multipartbody .= $MPboundary . "\r\n";
                $multipartbody .= 'Content-Disposition: form-data; name="' . $parameter . '"; filename="' . $filename . '"' . "\r\n";
                $multipartbody .= "Content-Type: image/unknown\r\n\r\n";
                $multipartbody .= $content . "\r\n";
            } else {
                $multipartbody .= $MPboundary . "\r\n";
                $multipartbody .= 'content-disposition: form-data; name="' . $parameter . "\"\r\n\r\n";
                $multipartbody .= $value . "\r\n";
            }
        }

        $multipartbody .= $endMPboundary;
        return $multipartbody;
    }

}

/**
 * ???????V2
 *
 * ??????????saetv2.ex.class.php <br />
 *
 * @package sae
 * @author Easy Chen, Elmer Zhang,Lazypeople
 * @version 1.0
 */
class SaeTClientV2 {

    /**
     * ????
     * 
     * @access public
     * @param mixed $akey ????????APP KEY
     * @param mixed $skey ????????APP SECRET
     * @param mixed $access_token OAuth?????token
     * @param mixed $refresh_token OAuth?????token secret
     * @return void
     */
    function __construct($akey, $skey, $access_token, $refresh_token = NULL) {
        $this->oauth = new SaeTOAuthV2($akey, $skey, $access_token, $refresh_token);
    }

    /**
     * ??????
     *
     * ????????SDK????????API????POST Data?Headers????????????????
     *
     * @access public
     * @param bool $enable ????????
     * @return void
     */
    function set_debug($enable) {
        $this->oauth->debug = $enable;
    }

    /**
     * ????IP
     *
     * SDK??????$_SERVER['REMOTE_ADDR']????IP??????API????IP???Request Header????????$_SERVER['REMOTE_ADDR']???IP????IP????????IP?????SAE?Cron?TaskQueue?????????????????IP????API?????????API?????????????????IP????????
     *
     * @access public
     * @param string $ip ??IP
     * @return bool IP???IP???????false?????true
     */
    function set_remote_ip($ip) {
        if (ip2long($ip) !== false) {
            $this->oauth->remote_ip = $ip;
            return true;
        } else {
            return false;
        }
    }

    /**
     * ???????????
     *
     * ??API?{@link http://open.weibo.com/wiki/2/statuses/public_timeline statuses/public_timeline}
     *
     * @access public
     * @param int $count ?????????????50?
     * @param int $page ???????????1?
     * @param int $base_app ?????????????0?????????1?????????????0?
     * @return array
     */
    function public_timeline($page = 1, $count = 50, $base_app = 0) {
        $params = array();
        $params['count'] = intval($count);
        $params['page'] = intval($page);
        $params['base_app'] = intval($base_app);
        return $this->oauth->get('statuses/public_timeline', $params); //??????bug????
    }

    /**
     * ???????????????????????
     *
     * ???????????????????????????? http://weibo.com ??“????”??????????friends_timeline()
     * <br />??API?{@link http://open.weibo.com/wiki/2/statuses/home_timeline statuses/home_timeline}
     * 
     * @access public
     * @param int $page ???????????????????????????????????????????????????????????????????1000???????1????
     * @param int $count ????????????50????200????
     * @param int $since_id ???????????ID?since_id?????????since_id???????????????
     * @param int $max_id ??????????ID?????max_id?????????
     * @param int $base_app ?????????????0?????????1?????????????0?
     * @param int $feature ????ID?0????1????2????3????4???????0?
     * @return array
     */
    function home_timeline($page = 1, $count = 50, $since_id = 0, $max_id = 0, $base_app = 0, $feature = 0) {
        $params = array();
        if ($since_id) {
            $this->id_format($since_id);
            $params['since_id'] = $since_id;
        }
        if ($max_id) {
            $this->id_format($max_id);
            $params['max_id'] = $max_id;
        }
        $params['count'] = intval($count);
        $params['page'] = intval($page);
        $params['base_app'] = intval($base_app);
        $params['feature'] = intval($feature);

        return $this->oauth->get('statuses/home_timeline', $params);
    }

    /**
     * ???????????????????????
     *
     * ???????????????????????????? http://weibo.com ??“????”??????????home_timeline()
     * <br />??API?{@link http://open.weibo.com/wiki/2/statuses/friends_timeline statuses/friends_timeline}
     * 
     * @access public
     * @param int $page ???????????????????????????????????????????????????????????????????1000???????1????
     * @param int $count ????????????50????200????
     * @param int $since_id ???????????ID?since_id?????????since_id???????????????
     * @param int $max_id ??????????ID?????max_id?????????
     * @param int $base_app ??????????????1?????????0?????????0????
     * @param int $feature ?????0???1???2???3???4??. ?????????????????0????
     * @return array
     */
    function friends_timeline($page = 1, $count = 50, $since_id = 0, $max_id = 0, $base_app = 0, $feature = 0) {
        return $this->home_timeline($since_id, $max_id, $count, $page, $base_app, $feature);
    }

    /**
     * ?????????????
     *
     * ??????????n????????????????????????????????????????
     * <br />??API?{@link http://open.weibo.com/wiki/2/statuses/user_timeline statuses/user_timeline}
     * 
     * @access public
     * @param int $page ??
     * @param int $count ???????????????200????50?
     * @param mixed $uid ????UID?????
     * @param int $since_id ???????????ID?since_id?????????since_id???????????????
     * @param int $max_id ??????????ID?????max_id?????????????????
     * @param int $base_app ??????????????1?????????0?????????0?
     * @param int $feature ????ID?0????1????2????3????4???????0?
     * @param int $trim_user ????user?????0??????user???1?user?????uid????0?
     * @return array
     */
    function user_timeline_by_id($uid = NULL, $page = 1, $count = 50, $since_id = 0, $max_id = 0, $feature = 0, $trim_user = 0, $base_app = 0) {
        $params = array();
        $params['uid'] = $uid;
        if ($since_id) {
            $this->id_format($since_id);
            $params['since_id'] = $since_id;
        }
        if ($max_id) {
            $this->id_format($max_id);
            $params['max_id'] = $max_id;
        }
        $params['base_app'] = intval($base_app);
        $params['feature'] = intval($feature);
        $params['count'] = intval($count);
        $params['page'] = intval($page);
        $params['trim_user'] = intval($trim_user);

        return $this->oauth->get('statuses/user_timeline', $params);
    }

    /**
     * ?????????????
     *
     * ??????????n????????????????????????????????????????
     * <br />??API?{@link http://open.weibo.com/wiki/2/statuses/user_timeline statuses/user_timeline}
     * 
     * @access public
     * @param string $screen_name ??????????????UID??????????????????????????? 
     * @param int $page ??
     * @param int $count ???????????????200????50?
     * @param int $since_id ???????????ID?since_id?????????since_id???????????????
     * @param int $max_id ??????????ID?????max_id?????????????????
     * @param int $feature ????ID?0????1????2????3????4???????0?
     * @param int $trim_user ????user?????0??????user???1?user?????uid????0?
     * @param int $base_app ??????????????1?????????0?????????0?
     * @return array
     */
    function user_timeline_by_name($screen_name = NULL, $page = 1, $count = 50, $since_id = 0, $max_id = 0, $feature = 0, $trim_user = 0, $base_app = 0) {
        $params = array();
        $params['screen_name'] = $screen_name;
        if ($since_id) {
            $this->id_format($since_id);
            $params['since_id'] = $since_id;
        }
        if ($max_id) {
            $this->id_format($max_id);
            $params['max_id'] = $max_id;
        }
        $params['base_app'] = intval($base_app);
        $params['feature'] = intval($feature);
        $params['count'] = intval($count);
        $params['page'] = intval($page);
        $params['trim_user'] = intval($trim_user);

        return $this->oauth->get('statuses/user_timeline', $params);
    }

    /**
     * ????????????timeline
     *
     * ??API?{@link http://open.weibo.com/wiki/2/statuses/timeline_batch statuses/timeline_batch}
     *
     * @param string $screen_name  ??????????????????????20?
     * @param int    $count        ?????????????50?
     * @param int    $page  ???????????1? 
     * @param int    $base_app  ?????????????0?????????1?????????????0?
     * @param int    $feature   ????ID?0????1????2????3????4???????0?
     * @return array
     */
    function timeline_batch_by_name($screen_name, $page = 1, $count = 50, $feature = 0, $base_app = 0) {
        $params = array();
        if (is_array($screen_name) && !empty($screen_name)) {
            $params['screen_name'] = join(',', $screen_name);
        } else {
            $params['screen_name'] = $screen_name;
        }
        $params['count'] = intval($count);
        $params['page'] = intval($page);
        $params['base_app'] = intval($base_app);
        $params['feature'] = intval($feature);
        return $this->oauth->get('statuses/timeline_batch', $params);
    }

    /**
     * ????????????timeline
     *
     * ??API?{@link http://open.weibo.com/wiki/2/statuses/timeline_batch statuses/timeline_batch}
     *
     * @param string $uids  ???????ID?????????????20??
     * @param int    $count        ?????????????50?
     * @param int    $page  ???????????1? 
     * @param int    $base_app  ?????????????0?????????1?????????????0?
     * @param int    $feature   ????ID?0????1????2????3????4???????0?
     * @return array
     */
    function timeline_batch_by_id($uids, $page = 1, $count = 50, $feature = 0, $base_app = 0) {
        $params = array();
        if (is_array($uids) && !empty($uids)) {
            foreach ($uids as $k => $v) {
                $this->id_format($uids[$k]);
            }
            $params['uids'] = join(',', $uids);
        } else {
            $params['uids'] = $uids;
        }
        $params['count'] = intval($count);
        $params['page'] = intval($page);
        $params['base_app'] = intval($base_app);
        $params['feature'] = intval($feature);
        return $this->oauth->get('statuses/timeline_batch', $params);
    }

    /**
     * ?????????????n???????????????????????? 
     *
     * ??API?{@link http://open.weibo.com/wiki/2/statuses/repost_timeline statuses/repost_timeline}
     * 
     * @access public
     * @param int $sid ??????????????ID?
     * @param int $page ???????? 
     * @param int $count ???????????????200????50????
     * @param int $since_id ???????????ID?since_id??????since_id??????????
     * @param int $max_id ??????????ID?????max_id???????
     * @param int $filter_by_author ???????0????1???????2????????0?
     * @return array
     */
    function repost_timeline($sid, $page = 1, $count = 50, $since_id = 0, $max_id = 0, $filter_by_author = 0) {
        $this->id_format($sid);

        $params = array();
        $params['id'] = $sid;
        if ($since_id) {
            $this->id_format($since_id);
            $params['since_id'] = $since_id;
        }
        if ($max_id) {
            $this->id_format($max_id);
            $params['max_id'] = $max_id;
        }
        $params['filter_by_author'] = intval($filter_by_author);

        return $this->request_with_pager('statuses/repost_timeline', $page, $count, $params);
    }

    /**
     * ???????????n?????
     *
     * ??API?{@link http://open.weibo.com/wiki/2/statuses/repost_by_me statuses/repost_by_me}
     * 
     * @access public
     * @param int $page ???????? 
     * @param int $count  ???????????????200????50????
     * @param int $since_id ???????????ID?since_id??????since_id??????????
     * @param int $max_id  ??????????ID?????max_id???????
     * @return array
     */
    function repost_by_me($page = 1, $count = 50, $since_id = 0, $max_id = 0) {
        $params = array();
        if ($since_id) {
            $this->id_format($since_id);
            $params['since_id'] = $since_id;
        }
        if ($max_id) {
            $this->id_format($max_id);
            $params['max_id'] = $max_id;
        }

        return $this->request_with_pager('statuses/repost_by_me', $page, $count, $params);
    }

    /**
     * ??@?????????
     *
     * ????n????????????????@username??????
     * <br />??API?{@link http://open.weibo.com/wiki/2/statuses/mentions statuses/mentions}
     * 
     * @access public
     * @param int $page ?????????
     * @param int $count ?????????????????????200????50?
     * @param int $since_id ???????????ID?since_id?????????since_id???????????????
     * @param int $max_id ??????????ID?????max_id?????????????????
     * @param int $filter_by_author ???????0????1???????2????????0?
     * @param int $filter_by_source ???????0????1??????2?????????0?
     * @param int $filter_by_type ???????0??????1??????????0?
     * @return array
     */
    function mentions($page = 1, $count = 50, $since_id = 0, $max_id = 0, $filter_by_author = 0, $filter_by_source = 0, $filter_by_type = 0) {
        $params = array();
        if ($since_id) {
            $this->id_format($since_id);
            $params['since_id'] = $since_id;
        }
        if ($max_id) {
            $this->id_format($max_id);
            $params['max_id'] = $max_id;
        }
        $params['filter_by_author'] = $filter_by_author;
        $params['filter_by_source'] = $filter_by_source;
        $params['filter_by_type'] = $filter_by_type;

        return $this->request_with_pager('statuses/mentions', $page, $count, $params);
    }

    /**
     * ??ID??????????
     *
     * ????ID????????????????
     * <br />??API?{@link http://open.weibo.com/wiki/2/statuses/show statuses/show}
     * 
     * @access public
     * @param int $id ?????????ID, ?ID??????
     * @return array
     */
    function show_status($id) {
        $this->id_format($id);
        $params = array();
        $params['id'] = $id;
        return $this->oauth->get('statuses/show', $params);
    }

    /**
     * ????id????????
     *
     * ??API?{@link http://open.weibo.com/wiki/2/statuses/show_batch statuses/show_batch}
     *
     * @param string $ids ???????ID??????????????50??
     * @return array
     */
    function show_batch($ids) {
        $params = array();
        if (is_array($ids) && !empty($ids)) {
            foreach ($ids as $k => $v) {
                $this->id_format($ids[$k]);
            }
            $params['ids'] = join(',', $ids);
        } else {
            $params['ids'] = $ids;
        }
        return $this->oauth->get('statuses/show_batch', $params);
    }

    /**
     * ???????????ID???MID
     *
     * ??API?{@link http://open.weibo.com/wiki/2/statuses/querymid statuses/querymid}
     *
     * @param int|string $id  ??????????????ID????????????????????20??
     * @param int $type  ?????1????2????3???????1?
     * @param int $is_batch ?????????0???1??????0?
     * @return array
     */
    function querymid($id, $type = 1, $is_batch = 0) {
        $params = array();
        $params['id'] = $id;
        $params['type'] = intval($type);
        $params['is_batch'] = intval($is_batch);
        return $this->oauth->get('statuses/querymid', $params);
    }

    /**
     * ???????????MID???ID
     *
     * ??API?{@link http://open.weibo.com/wiki/2/statuses/queryid statuses/queryid}
     *
     * @param int|string $mid  ??????????????MID????????????????????20??
     * @param int $type  ?????1????2????3???????1?
     * @param int $is_batch ?????????0???1??????0?
     * @param int $inbox  ????????MID???????????0?????1????????0 ?
     * @param int $isBase62 MID???base62???0???1??????0?
     * @return array
     */
    function queryid($mid, $type = 1, $is_batch = 0, $inbox = 0, $isBase62 = 0) {
        $params = array();
        $params['mid'] = $mid;
        $params['type'] = intval($type);
        $params['is_batch'] = intval($is_batch);
        $params['inbox'] = intval($inbox);
        $params['isBase62'] = intval($isBase62);
        return $this->oauth->get('statuses/queryid', $params);
    }

    /**
     * ????????????????
     *
     * ??API?{@link http://open.weibo.com/wiki/2/statuses/hot/repost_daily statuses/hot/repost_daily}
     *
     * @param int $count ?????????????50????20?
     * @param int $base_app ?????????????0?????????1?????????????0?
     * @return array
     */
    function repost_daily($count = 20, $base_app = 0) {
        $params = array();
        $params['count'] = intval($count);
        $params['base_app'] = intval($base_app);
        return $this->oauth->get('statuses/hot/repost_daily', $params);
    }

    /**
     * ????????????????
     *
     * ??API?{@link http://open.weibo.com/wiki/2/statuses/hot/repost_weekly statuses/hot/repost_weekly}
     *
     * @param int $count ?????????????50????20?
     * @param int $base_app ?????????????0?????????1?????????????0?
     * @return array
     */
    function repost_weekly($count = 20, $base_app = 0) {
        $params = array();
        $params['count'] = intval($count);
        $params['base_app'] = intval($base_app);
        return $this->oauth->get('statuses/hot/repost_weekly', $params);
    }

    /**
     * ????????????????
     *
     * ??API?{@link http://open.weibo.com/wiki/2/statuses/hot/comments_daily statuses/hot/comments_daily}
     *
     * @param int $count ?????????????50????20?
     * @param int $base_app ?????????????0?????????1?????????????0?
     * @return array
     */
    function comments_daily($count = 20, $base_app = 0) {
        $params = array();
        $params['count'] = intval($count);
        $params['base_app'] = intval($base_app);
        return $this->oauth->get('statuses/hot/comments_daily', $params);
    }

    /**
     * ????????????????
     *
     * ??API?{@link http://open.weibo.com/wiki/2/statuses/hot/comments_weekly statuses/hot/comments_weekly}
     *
     * @param int $count ?????????????50????20?
     * @param int $base_app ?????????????0?????????1?????????????0?
     * @return array
     */
    function comments_weekly($count = 20, $base_app = 0) {
        $params = array();
        $params['count'] = intval($count);
        $params['base_app'] = intval($base_app);
        return $this->oauth->get('statuses/hot/comments_weekly', $params);
    }

    /**
     * ?????????
     *
     * ???????????????????????????????
     * <br />??API?{@link http://open.weibo.com/wiki/2/statuses/repost statuses/repost}
     * 
     * @access public
     * @param int $sid ?????ID
     * @param string $text ???????????
     * @param int $is_comment ?????????????0???1?????????2????????3????????0?
     * @return array
     */
    function repost($sid, $text = NULL, $is_comment = 0) {
        $this->id_format($sid);

        $params = array();
        $params['id'] = $sid;
        $params['is_comment'] = $is_comment;
        if ($text)
            $params['status'] = $text;

        return $this->oauth->post('statuses/repost', $params);
    }

    /**
     * ??????
     * 
     * ??ID??????????????????????
     * <br />??API?{@link http://open.weibo.com/wiki/2/statuses/destroy statuses/destroy}
     * 
     * @access public
     * @param int $id ??????ID
     * @return array
     */
    function delete($id) {
        return $this->destroy($id);
    }

    /**
     * ??????
     *
     * ????????????????????
     * <br />??API?{@link http://open.weibo.com/wiki/2/statuses/destroy statuses/destroy}
     * 
     * @access public
     * @param int $id ??????ID
     * @return array
     */
    function destroy($id) {
        $this->id_format($id);
        $params = array();
        $params['id'] = $id;
        return $this->oauth->post('statuses/destroy', $params);
    }

    /**
     * ????
     *
     * ?????????
     * <br />???lat?long??????????????????????????????????geo_enabled=true????????????
     * <br />??????????????????????????????????????????400??????????“40025:Error: repeated weibo text!“? 
     * <br />??API?{@link http://open.weibo.com/wiki/2/statuses/update statuses/update}
     * 
     * @access public
     * @param string $status ????????????????140???, ????400???
     * @param float $lat ????????????????????? -90.0?+90.0, +????????
     * @param float $long ???????-180.0?+180.0, +????????
     * @param mixed $annotations ????????????????????????????????????????????????????????json???????????????512?????????????json_encode????????512????????????????'[{"type2":123}, {"a":"b", "c":"d"}]'?array(array("type2"=>123), array("a"=>"b", "c"=>"d"))?
     * @param int $visible    ???????0???????1???????2??????3???????????0
     * @return array
     */
    function update($status, $lat = NULL, $long = NULL, $annotations = NULL, $visible = 0) {
        $params = array();
        $params['status'] = $status;
        $params['visible'] = $visible;
        if ($lat) {
            $params['lat'] = floatval($lat);
        }
        if ($long) {
            $params['long'] = floatval($long);
        }
        if (is_string($annotations)) {
            $params['annotations'] = $annotations;
        } elseif (is_array($annotations)) {
            $params['annotations'] = json_encode($annotations);
        }

        return $this->oauth->post('statuses/update', $params);
    }

    /**
     * ??????
     *
     * ????????????????????<5M? 
     * <br />???lat?long??????????????????????????????????geo_enabled=true????????????
     * <br />??API?{@link http://open.weibo.com/wiki/2/statuses/upload statuses/upload}
     * 
     * @access public
     * @param string $status ????????????????140???, ????400???
     * @param string $pic_path ????????, ??url?[???png/jpg/gif????, ???????get_image_mime??]
     * @param float $lat ????????????????????? -90.0?+90.0, +????????
     * @param float $long ????????????-180.0?+180.0, +????????
     * @param int $visible    ???????0???????1???????2??????3???????????0
     * @return array
     */
    function upload($status, $pic_path, $lat = NULL, $long = NULL, $visible = 0) {
        $params = array();
        $params['status'] = $status;
        $params['pic'] = '@' . $pic_path;
        $params['visible'] = $visible;
        if ($lat) {
            $params['lat'] = floatval($lat);
        }
        if ($long) {
            $params['long'] = floatval($long);
        }

        return $this->oauth->post('statuses/upload', $params, true);
    }

    /**
     * ????????????
     *
     * ???????????<5M? 
     * <br />??API?{@link http://open.weibo.com/wiki/2/statuses/share}
     * 
     * @access public
     * @param string $status ????????????????URLencode??????140???????????“#???#”?????????????????????????URL???URL?????????????????URL???????“???? ? ???? ? ???????? ? ????”???????
     * @param string $pic ????????????????JPEG?GIF?PNG????????????<5M???????POST???????????multipart/form-data????????
     * @param string $rip ????????????IP????211.156.0.1????
     * @return array
     */
    function share($status, $pic = false, $rip = false) {
        $params = array();
        $params['status'] = $status;
        $with_media = false;
        if ($pic) {
            $params['pic'] = '@' . $pic;
            $with_media = true;
        }
        if ($rip) {
            $params['rip'] = $rip;
        }

        return $this->oauth->post('statuses/share', $params, $with_media);
    }

    /**
     * ??????URL?????????????????
     *
     * ??API?{@link http://open.weibo.com/wiki/2/statuses/upload_url_text statuses/upload_url_text}
     *
     * @param string $status  ????????????????140????
     * @param int $visible    ???????0???????1???????2??????3???????????0
     * @param string $list_id ???????????ID????visible???3???????
     * @param string $pic_id ???????pid?????????????????????9??
     * @param float $lat ????????-90.0?+90.0?+????????0.0?
     * @param float $long ????????-180.0?+180.0?+????????0.0?
     * @param string $annotations ?????????????????????????????????????????????????
     *                            ???json???????????????512?????????????
     * @param string $url    ???URL??????http???
     * @return array
     */
    function upload_url_text($status, $url, $visible = 0, $list_id = NULL, $pic_id = NULL, $lat = NULL, $long = NULL, $annotations = NULL) {
        $params = array();
        $params['status'] = $status;
        $params['url'] = $url;
        $params['visible'] = $visible;
        if (!is_null($list_id)) {
            $params['list_id'] = $list_id;
        }
        if (!is_null($pic_id)) {
            $params['pic_id'] = $pic_id;
        }
        if (!is_null($lat)) {
            $params['lat'] = $lat;
        }
        if (!is_null($long)) {
            $params['long'] = $long;
        }
        if (!is_null($annotations)) {
            $params['annotations'] = $annotations;
        }
        return $this->oauth->post('statuses/upload_url_text', $params, true);
    }

    /**
     * ??????
     *
     * ????????????????????????????????????????????
     * <br />??API?{@link http://open.weibo.com/wiki/2/emotions emotions}
     * 
     * @access public
     * @param string $type ?????"face":?????"ani"??????"cartoon"?????????"face"????
     * @param string $language ?????"cnname"???"twname"??????"cnname"???
     * @return array
     */
    function emotions($type = "face", $language = "cnname") {
        $params = array();
        $params['type'] = $type;
        $params['language'] = $language;
        return $this->oauth->get('emotions', $params);
    }

    /**
     * ????ID???????????
     *
     * ??API?{@link http://open.weibo.com/wiki/2/comments/show comments/show}
     *
     * @param int $sid ???????ID?
     * @param int $page ???????????1?
     * @param int $count ?????????????50?
     * @param int $since_id ??????????ID?since_id???????since_id???????????0?
     * @param int $max_id  ??????????ID?????max_id???????0?
     * @param int $filter_by_author ???????0????1???????2????????0?
     * @return array
     */
    function get_comments_by_sid($sid, $page = 1, $count = 50, $since_id = 0, $max_id = 0, $filter_by_author = 0) {
        $params = array();
        $this->id_format($sid);
        $params['id'] = $sid;
        if ($since_id) {
            $this->id_format($since_id);
            $params['since_id'] = $since_id;
        }
        if ($max_id) {
            $this->id_format($max_id);
            $params['max_id'] = $max_id;
        }
        $params['count'] = $count;
        $params['page'] = $page;
        $params['filter_by_author'] = $filter_by_author;
        return $this->oauth->get('comments/show', $params);
    }

    /**
     * ????????????????
     *
     * ??API?{@link http://open.weibo.com/wiki/2/comments/by_me comments/by_me}
     *
     * @param int $since_id ??????????ID?since_id???????since_id???????????0?
     * @param int $max_id ??????????ID?????max_id???????0?
     * @param int $count  ?????????????50?
     * @param int $page ???????????1?
     * @param int $filter_by_source ???????0????1?????????2????????????0?
     * @return array
     */
    function comments_by_me($page = 1, $count = 50, $since_id = 0, $max_id = 0, $filter_by_source = 0) {
        $params = array();
        if ($since_id) {
            $this->id_format($since_id);
            $params['since_id'] = $since_id;
        }
        if ($max_id) {
            $this->id_format($max_id);
            $params['max_id'] = $max_id;
        }
        $params['count'] = $count;
        $params['page'] = $page;
        $params['filter_by_source'] = $filter_by_source;
        return $this->oauth->get('comments/by_me', $params);
    }

    /**
     * ?????????????????
     *
     * ??API?{@link http://open.weibo.com/wiki/2/comments/to_me comments/to_me}
     *
     * @param int $since_id ??????????ID?since_id???????since_id???????????0?
     * @param int $max_id  ??????????ID?????max_id???????0?
     * @param int $count ?????????????50?
     * @param int $page ???????????1?
     * @param int $filter_by_author ???????0????1???????2????????0?
     * @param int $filter_by_source ???????0????1?????????2????????????0?
     * @return array
     */
    function comments_to_me($page = 1, $count = 50, $since_id = 0, $max_id = 0, $filter_by_author = 0, $filter_by_source = 0) {
        $params = array();
        if ($since_id) {
            $this->id_format($since_id);
            $params['since_id'] = $since_id;
        }
        if ($max_id) {
            $this->id_format($max_id);
            $params['max_id'] = $max_id;
        }
        $params['count'] = $count;
        $params['page'] = $page;
        $params['filter_by_author'] = $filter_by_author;
        $params['filter_by_source'] = $filter_by_source;
        return $this->oauth->get('comments/to_me', $params);
    }

    /**
     * ????(???)
     *
     * ????n??????????
     * <br />??API?{@link http://open.weibo.com/wiki/2/comments/timeline comments/timeline}
     * 
     * @access public
     * @param int $page ??
     * @param int $count ???????????????200????50?
     * @param int $since_id ???????????ID?since_id??????since_id??????????
     * @param int $max_id ??????????ID?????max_id???????
     * @return array
     */
    function comments_timeline($page = 1, $count = 50, $since_id = 0, $max_id = 0) {
        $params = array();
        if ($since_id) {
            $this->id_format($since_id);
            $params['since_id'] = $since_id;
        }
        if ($max_id) {
            $this->id_format($max_id);
            $params['max_id'] = $max_id;
        }

        return $this->request_with_pager('comments/timeline', $page, $count, $params);
    }

    /**
     * ??????????????????@????
     *
     * ??API?{@link http://open.weibo.com/wiki/2/comments/mentions comments/mentions}
     *
     * @param int $since_id ??????????ID?since_id???????since_id???????????0?
     * @param int $max_id  ??????????ID?????max_id???????0?
     * @param int $count ?????????????50?
     * @param int $page ???????????1?
     * @param int $filter_by_author  ???????0????1???????2????????0?
     * @param int $filter_by_source ???????0????1?????????2????????????0?
     * @return array
     */
    function comments_mentions($page = 1, $count = 50, $since_id = 0, $max_id = 0, $filter_by_author = 0, $filter_by_source = 0) {
        $params = array();
        $params['since_id'] = $since_id;
        $params['max_id'] = $max_id;
        $params['count'] = $count;
        $params['page'] = $page;
        $params['filter_by_author'] = $filter_by_author;
        $params['filter_by_source'] = $filter_by_source;
        return $this->oauth->get('comments/mentions', $params);
    }

    /**
     * ????ID????????
     *
     * ??API?{@link http://open.weibo.com/wiki/2/comments/show_batch comments/show_batch}
     *
     * @param string $cids ?????????ID???????????50
     * @return array
     */
    function comments_show_batch($cids) {
        $params = array();
        if (is_array($cids) && !empty($cids)) {
            foreach ($cids as $k => $v) {
                $this->id_format($cids[$k]);
            }
            $params['cids'] = join(',', $cids);
        } else {
            $params['cids'] = $cids;
        }
        return $this->oauth->get('comments/show_batch', $params);
    }

    /**
     * ?????????
     *
     * ??API?{@link http://open.weibo.com/wiki/2/comments/create comments/create}
     *
     * @param string $comment ??????????140????
     * @param int $id ???????ID?
     * @param int $comment_ori ??????????????????0???1??????0?
     * @return array
     */
    function send_comment($id, $comment, $comment_ori = 0) {
        $params = array();
        $params['comment'] = $comment;
        $this->id_format($id);
        $params['id'] = $id;
        $params['comment_ori'] = $comment_ori;
        return $this->oauth->post('comments/create', $params);
    }

    /**
     * ??????????????
     *
     * ??????????????????????????????????
     * <br />??API?{@link http://open.weibo.com/wiki/2/statuses/comment_destroy statuses/comment_destroy}
     * 
     * @access public
     * @param int $cid ??????id
     * @return array
     */
    function comment_destroy($cid) {
        $params = array();
        $params['cid'] = $cid;
        return $this->oauth->post('comments/destroy', $params);
    }

    /**
     * ????ID??????
     *
     * ??????????????????????????????????
     * <br />??API?{@link http://open.weibo.com/wiki/2/comments/destroy_batch comments/destroy_batch}
     *
     * @access public
     * @param string $ids ???????ID???????????20??
     * @return array
     */
    function comment_destroy_batch($ids) {
        $params = array();
        if (is_array($ids) && !empty($ids)) {
            foreach ($ids as $k => $v) {
                $this->id_format($ids[$k]);
            }
            $params['cids'] = join(',', $ids);
        } else {
            $params['cids'] = $ids;
        }
        return $this->oauth->post('comments/destroy_batch', $params);
    }

    /**
     * ??????
     *
     * ??????????????????/??????????????
     * <br />??API?{@link http://open.weibo.com/wiki/2/comments/reply comments/reply}
     * 
     * @access public
     * @param int $sid ??id
     * @param string $text ?????
     * @param int $cid ??id
     * @param int $without_mention 1?????????“??@???”?0????????“??@???”.???0.
     * @param int $comment_ori	  ??????????????????0???1??????0?
     * @return array
     */
    function reply($sid, $text, $cid, $without_mention = 0, $comment_ori = 0) {
        $this->id_format($sid);
        $this->id_format($cid);
        $params = array();
        $params['id'] = $sid;
        $params['comment'] = $text;
        $params['cid'] = $cid;
        $params['without_mention'] = $without_mention;
        $params['comment_ori'] = $comment_ori;

        return $this->oauth->post('comments/reply', $params);
    }

    /**
     * ????UID?????????
     *
     * ???UID???????????????????????????
     * <br />??API?{@link http://open.weibo.com/wiki/2/users/show users/show}
     * 
     * @access public
     * @param int  $uid ??UID?
     * @return array
     */
    function show_user_by_id($uid) {
        $params = array();
        if ($uid !== NULL) {
            $this->id_format($uid);
            $params['uid'] = $uid;
        }

        return $this->oauth->get('users/show', $params);
    }

    /**
     * ????UID?????????
     *
     * ???UID???????????????????????????
     * <br />??API?{@link http://open.weibo.com/wiki/2/users/show users/show}
     * 
     * @access public
     * @param string  $screen_name ??UID?
     * @return array
     */
    function show_user_by_name($screen_name) {
        $params = array();
        $params['screen_name'] = $screen_name;

        return $this->oauth->get('users/show', $params);
    }

    /**
     * ????????????????????????
     *
     * ??API?{@link http://open.weibo.com/wiki/2/users/domain_show users/domain_show}
     * 
     * @access public
     * @param mixed $domain ??????????lazypeople????http://weibo.com/lazypeople
     * @return array
     */
    function domain_show($domain) {
        $params = array();
        $params['domain'] = $domain;
        return $this->oauth->get('users/domain_show', $params);
    }

    /**
     * ?????????uids
     *
     * ??API?{@link http://open.weibo.com/wiki/2/users/show_batch users/show_batch}
     *
     * @param string $uids ???????ID?????????????20??
     * @return array
     */
    function users_show_batch_by_id($uids) {
        $params = array();
        if (is_array($uids) && !empty($uids)) {
            foreach ($uids as $k => $v) {
                $this->id_format($uids[$k]);
            }
            $params['uids'] = join(',', $uids);
        } else {
            $params['uids'] = $uids;
        }
        return $this->oauth->get('users/show_batch', $params);
    }

    /**
     * ?????????screen_name
     *
     * ??API?{@link http://open.weibo.com/wiki/2/users/show_batch users/show_batch}
     *
     * @param string  $screen_name ??????????????????????20??
     * @return array
     */
    function users_show_batch_by_name($screen_name) {
        $params = array();
        if (is_array($screen_name) && !empty($screen_name)) {
            $params['screen_name'] = join(',', $screen_name);
        } else {
            $params['screen_name'] = $screen_name;
        }
        return $this->oauth->get('users/show_batch', $params);
    }

    /**
     * ?????????
     *
     * ??????cursor???????????5000???id
     * <br />??API?{@link http://open.weibo.com/wiki/2/friendships/friends friendships/friends}
     * 
     * @access public
     * @param int $cursor ?????????????????next_cursor?????previous_cursor????0?
     * @param int $count ?????????????50??????200?
     * @param int $uid  ???????ID?
     * @return array
     */
    function friends_by_id($uid, $cursor = 0, $count = 50) {
        $params = array();
        $params['cursor'] = $cursor;
        $params['count'] = $count;
        $params['uid'] = $uid;

        return $this->oauth->get('friendships/friends', $params);
    }

    /**
     * ?????????
     *
     * ??????cursor???????????5000???id
     * <br />??API?{@link http://open.weibo.com/wiki/2/friendships/friends friendships/friends}
     * 
     * @access public
     * @param int $cursor ?????????????????next_cursor?????previous_cursor????0?
     * @param int $count ?????????????50??????200?
     * @param string $screen_name  ??????? screen_name
     * @return array
     */
    function friends_by_name($screen_name, $cursor = 0, $count = 50) {
        $params = array();
        $params['cursor'] = $cursor;
        $params['count'] = $count;
        $params['screen_name'] = $screen_name;
        return $this->oauth->get('friendships/friends', $params);
    }

    /**
     * ????????????????
     *
     * ??API?{@link http://open.weibo.com/wiki/2/friendships/friends/in_common friendships/friends/in_common}
     *
     * @param int $uid  ?????????????UID
     * @param int $suid  ?????????????UID???????????
     * @param int $count  ?????????????50?
     * @param int $page  ???????????1?
     * @return array
     */
    function friends_in_common($uid, $suid = NULL, $page = 1, $count = 50) {
        $params = array();
        $params['uid'] = $uid;
        $params['suid'] = $suid;
        $params['count'] = $count;
        $params['page'] = $page;
        return $this->oauth->get('friendships/friends/in_common', $params);
    }

    /**
     * ?????????????????
     *
     * ??API?{@link http://open.weibo.com/wiki/2/friendships/friends/bilateral friendships/friends/bilateral}
     *
     * @param int $uid  ?????????????UID?
     * @param int $count  ?????????????50?
     * @param int $page  ???????????1?
     * @param int $sort  ?????0??????????????0?
     * @return array
     * */
    function bilateral($uid, $page = 1, $count = 50, $sort = 0) {
        $params = array();
        $params['uid'] = $uid;
        $params['count'] = $count;
        $params['page'] = $page;
        $params['sort'] = $sort;
        return $this->oauth->get('friendships/friends/bilateral', $params);
    }

    /**
     * ?????????uid??
     *
     * ??API?{@link http://open.weibo.com/wiki/2/friendships/friends/bilateral/ids friendships/friends/bilateral/ids}
     *
     * @param int $uid  ?????????????UID?
     * @param int $count ?????????????50?
     * @param int $page  ???????????1?
     * @param int $sort  ?????0??????????????0?
     * @return array
     * */
    function bilateral_ids($uid, $page = 1, $count = 50, $sort = 0) {
        $params = array();
        $params['uid'] = $uid;
        $params['count'] = $count;
        $params['page'] = $page;
        $params['sort'] = $sort;
        return $this->oauth->get('friendships/friends/bilateral/ids', $params);
    }

    /**
     * ?????????uid
     *
     * ??????cursor???????????5000???id
     * <br />??API?{@link http://open.weibo.com/wiki/2/friendships/friends/ids friendships/friends/ids}
     * 
     * @access public
     * @param int $cursor ?????????????????next_cursor?????previous_cursor????0?
     * @param int $count ?????????????????????5000, ????500?
     * @param int $uid ?????? UID????????
     * @return array
     */
    function friends_ids_by_id($uid, $cursor = 0, $count = 500) {
        $params = array();
        $this->id_format($uid);
        $params['uid'] = $uid;
        $params['cursor'] = $cursor;
        $params['count'] = $count;
        return $this->oauth->get('friendships/friends/ids', $params);
    }

    /**
     * ?????????uid
     *
     * ??????cursor???????????5000???id
     * <br />??API?{@link http://open.weibo.com/wiki/2/friendships/friends/ids friendships/friends/ids}
     * 
     * @access public
     * @param int $cursor ?????????????????next_cursor?????previous_cursor????0?
     * @param int $count ?????????????????????5000, ????500?
     * @param string $screen_name ??????? screen_name????????
     * @return array
     */
    function friends_ids_by_name($screen_name, $cursor = 0, $count = 500) {
        $params = array();
        $params['cursor'] = $cursor;
        $params['count'] = $count;
        $params['screen_name'] = $screen_name;
        return $this->oauth->get('friendships/friends/ids', $params);
    }

    /**
     * ???????????????????
     *
     * ??API?{@link http://open.weibo.com/wiki/2/friendships/friends/remark_batch friendships/friends/remark_batch}
     *
     * @param string $uids  ?????????UID??????????????50??
     * @return array
     * */
    function friends_remark_batch($uids) {
        $params = array();
        if (is_array($uids) && !empty($uids)) {
            foreach ($uids as $k => $v) {
                $this->id_format($uids[$k]);
            }
            $params['uids'] = join(',', $uids);
        } else {
            $params['uids'] = $uids;
        }
        return $this->oauth->get('friendships/friends/remark_batch', $params);
    }

    /**
     * ?????????
     *
     * ??API?{@link http://open.weibo.com/wiki/2/friendships/followers friendships/followers}
     *
     * @param int $uid  ???????UID
     * @param int $count ?????????????50??????200?
     * @param int $cursor false ?????????????????next_cursor?????previous_cursor????0?
     * @return array
     * */
    function followers_by_id($uid, $cursor = 0, $count = 50) {
        $params = array();
        $this->id_format($uid);
        $params['uid'] = $uid;
        $params['count'] = $count;
        $params['cursor'] = $cursor;
        return $this->oauth->get('friendships/followers', $params);
    }

    /**
     * ?????????
     *
     * ??API?{@link http://open.weibo.com/wiki/2/friendships/followers friendships/followers}
     *
     * @param string $screen_name  ??????????
     * @param int  $count ?????????????50??????200?
     * @param int  $cursor false ?????????????????next_cursor?????previous_cursor????0?
     * @return array
     * */
    function followers_by_name($screen_name, $cursor = 0, $count = 50) {
        $params = array();
        $params['screen_name'] = $screen_name;
        $params['count'] = $count;
        $params['cursor'] = $cursor;
        return $this->oauth->get('friendships/followers', $params);
    }

    /**
     * ?????????uid
     *
     * ??API?{@link http://open.weibo.com/wiki/2/friendships/followers friendships/followers}
     *
     * @param int $uid ???????UID
     * @param int $count ?????????????50??????200?
     * @param int $cursor ?????????????????next_cursor?????previous_cursor????0?
     * @return array
     * */
    function followers_ids_by_id($uid, $cursor = 0, $count = 50) {
        $params = array();
        $this->id_format($uid);
        $params['uid'] = $uid;
        $params['count'] = $count;
        $params['cursor'] = $cursor;
        return $this->oauth->get('friendships/followers/ids', $params);
    }

    /**
     * ?????????uid
     *
     * ??API?{@link http://open.weibo.com/wiki/2/friendships/followers friendships/followers}
     *
     * @param string $screen_name ???????screen_name
     * @param int $count ?????????????50??????200?
     * @param int $cursor ?????????????????next_cursor?????previous_cursor????0?
     * @return array
     * */
    function followers_ids_by_name($screen_name, $cursor = 0, $count = 50) {
        $params = array();
        $params['screen_name'] = $screen_name;
        $params['count'] = $count;
        $params['cursor'] = $cursor;
        return $this->oauth->get('friendships/followers/ids', $params);
    }

    /**
     * ??????
     *
     * ??API?{@link http://open.weibo.com/wiki/2/friendships/followers/active friendships/followers/active}
     *
     * @param int $uid ???????UID?
     * @param int $count ???????????20??????200?
     * @return array
     * */
    function followers_active($uid, $count = 20) {
        $param = array();
        $this->id_format($uid);
        $param['uid'] = $uid;
        $param['count'] = $count;
        return $this->oauth->get('friendships/followers/active', $param);
    }

    /**
     * ??????????????????????????
     *
     * ??API?{@link http://open.weibo.com/wiki/2/friendships/friends_chain/followers friendships/friends_chain/followers}
     *
     * @param int $uid ?????????UID?
     * @param int $count ?????????????50?
     * @param int $page ???????????1?
     * @return array
     * */
    function friends_chain_followers($uid, $page = 1, $count = 50) {
        $params = array();
        $this->id_format($uid);
        $params['uid'] = $uid;
        $params['count'] = $count;
        $params['page'] = $page;
        return $this->oauth->get('friendships/friends_chain/followers', $params);
    }

    /**
     * ?????????????
     *
     * ?????????????????http?400??
     * <br />??API?{@link http://open.weibo.com/wiki/2/friendships/show friendships/show}
     * 
     * @access public
     * @param mixed $target_id ????UID
     * @param mixed $source_id ???UID????????????
     * @return array
     */
    function is_followed_by_id($target_id, $source_id = NULL) {
        $params = array();
        $this->id_format($target_id);
        $params['target_id'] = $target_id;

        if ($source_id != NULL) {
            $this->id_format($source_id);
            $params['source_id'] = $source_id;
        }

        return $this->oauth->get('friendships/show', $params);
    }

    /**
     * ?????????????
     *
     * ?????????????????http?400??
     * <br />??API?{@link http://open.weibo.com/wiki/2/friendships/show friendships/show}
     * 
     * @access public
     * @param mixed $target_name ?????????
     * @param mixed $source_name ????????????????????
     * @return array
     */
    function is_followed_by_name($target_name, $source_name = NULL) {
        $params = array();
        $params['target_screen_name'] = $target_name;

        if ($source_name != NULL) {
            $params['source_screen_name'] = $source_name;
        }

        return $this->oauth->get('friendships/show', $params);
    }

    /**
     * ???????
     *
     * ??????????????????2000?????????????????????????????http 403??????????ID???400?
     * <br />??API?{@link http://open.weibo.com/wiki/2/friendships/create friendships/create}
     * 
     * @access public
     * @param int $uid ??????UID
     * @return array
     */
    function follow_by_id($uid) {
        $params = array();
        $this->id_format($uid);
        $params['uid'] = $uid;
        return $this->oauth->post('friendships/create', $params);
    }

    /**
     * ???????
     *
     * ???????????????????2000?????????????????????????????http 403??????????ID???400?
     * <br />??API?{@link http://open.weibo.com/wiki/2/friendships/create friendships/create}
     * 
     * @access public
     * @param string $screen_name ????????
     * @return array
     */
    function follow_by_name($screen_name) {
        $params = array();
        $params['screen_name'] = $screen_name;
        return $this->oauth->post('friendships/create', $params);
    }

    /**
     * ????UID??????
     *
     * ??API?{@link http://open.weibo.com/wiki/2/friendships/create_batch friendships/create_batch}
     *
     * @param string $uids ??????UID??????????????20??
     * @return array
     */
    function follow_create_batch($uids) {
        $params = array();
        if (is_array($uids) && !empty($uids)) {
            foreach ($uids as $k => $v) {
                $this->id_format($uids[$k]);
            }
            $params['uids'] = join(',', $uids);
        } else {
            $params['uids'] = $uids;
        }
        return $this->oauth->post('friendships/create_batch', $params);
    }

    /**
     * ???????
     *
     * ?????????????????????????????????????
     * <br />??API?{@link http://open.weibo.com/wiki/2/friendships/destroy friendships/destroy}
     * 
     * @access public
     * @param int $uid ????????UID
     * @return array
     */
    function unfollow_by_id($uid) {
        $params = array();
        $this->id_format($uid);
        $params['uid'] = $uid;
        return $this->oauth->post('friendships/destroy', $params);
    }

    /**
     * ???????
     *
     * ?????????????????????????????????????
     * <br />??API?{@link http://open.weibo.com/wiki/2/friendships/destroy friendships/destroy}
     * 
     * @access public
     * @param string $screen_name ??????????
     * @return array
     */
    function unfollow_by_name($screen_name) {
        $params = array();
        $params['screen_name'] = $screen_name;
        return $this->oauth->post('friendships/destroy', $params);
    }

    /**
     * ?????????????????????
     *
     * ???????????????????????????400???
     * <br />??API?{@link http://open.weibo.com/wiki/2/friendships/remark/update friendships/remark/update}
     * 
     * @access public
     * @param int $uid ???????????ID?
     * @param string $remark ?????
     * @return array
     */
    function update_remark($uid, $remark) {
        $params = array();
        $this->id_format($uid);
        $params['uid'] = $uid;
        $params['remark'] = $remark;
        return $this->oauth->post('friendships/remark/update', $params);
    }

    /**
     * ????????????
     *
     * ???????n????????????????????
     * <br />??API?{@link http://open.weibo.com/wiki/2/direct_messages direct_messages}
     * 
     * @access public
     * @param int $page ??
     * @param int $count ???????????????200????50?
     * @param int64 $since_id ??ID???since_id???since_id????????????
     * @param int64 $max_id ??ID???max_id(?????max_id)???????
     * @return array
     */
    function list_dm($page = 1, $count = 50, $since_id = 0, $max_id = 0) {
        $params = array();
        if ($since_id) {
            $this->id_format($since_id);
            $params['since_id'] = $since_id;
        }
        if ($max_id) {
            $this->id_format($max_id);
            $params['max_id'] = $max_id;
        }

        return $this->request_with_pager('direct_messages', $page, $count, $params);
    }

    /**
     * ???????????????
     *
     * ???????????50???????????????????
     * <br />??API?{@link http://open.weibo.com/wiki/2/direct_messages/sent direct_messages/sent}
     * 
     * @access public
     * @param int $page ??
     * @param int $count ???????????????200????50?
     * @param int64 $since_id ??ID???since_id???since_id????????????
     * @param int64 $max_id ??ID???max_id(?????max_id)???????
     * @return array
     */
    function list_dm_sent($page = 1, $count = 50, $since_id = 0, $max_id = 0) {
        $params = array();
        if ($since_id) {
            $this->id_format($since_id);
            $params['since_id'] = $since_id;
        }
        if ($max_id) {
            $this->id_format($max_id);
            $params['max_id'] = $max_id;
        }

        return $this->request_with_pager('direct_messages/sent', $page, $count, $params);
    }

    /**
     * ???????????????????????????????
     *
     * ??API?{@link http://open.weibo.com/wiki/2/direct_messages/user_list direct_messages/user_list}
     *
     * @param int $count  ?????????????20?
     * @param int $cursor ?????????????????next_cursor?????previous_cursor????0?
     * @return array
     */
    function dm_user_list($count = 20, $cursor = 0) {
        $params = array();
        $params['count'] = $count;
        $params['cursor'] = $cursor;
        return $this->oauth->get('direct_messages/user_list', $params);
    }

    /**
     * ??????????????
     *
     * ??API?{@link http://open.weibo.com/wiki/2/direct_messages/conversation direct_messages/conversation}
     *
     * @param int $uid ????????UID?
     * @param int $since_id ??????????ID?since_id???????since_id???????????0?
     * @param int $max_id  ??????????ID?????max_id???????0?
     * @param int $count ?????????????50?
     * @param int $page  ???????????1?
     * @return array
     */
    function dm_conversation($uid, $page = 1, $count = 50, $since_id = 0, $max_id = 0) {
        $params = array();
        $this->id_format($uid);
        $params['uid'] = $uid;
        if ($since_id) {
            $this->id_format($since_id);
            $params['since_id'] = $since_id;
        }
        if ($max_id) {
            $this->id_format($max_id);
            $params['max_id'] = $max_id;
        }
        $params['count'] = $count;
        $params['page'] = $page;
        return $this->oauth->get('direct_messages/conversation', $params);
    }

    /**
     * ????ID????????
     *
     * ??API?{@link http://open.weibo.com/wiki/2/direct_messages/show_batch direct_messages/show_batch}
     *
     * @param string  $dmids ???????ID?????????????50?
     * @return array
     */
    function dm_show_batch($dmids) {
        $params = array();
        if (is_array($dmids) && !empty($dmids)) {
            foreach ($dmids as $k => $v) {
                $this->id_format($dmids[$k]);
            }
            $params['dmids'] = join(',', $dmids);
        } else {
            $params['dmids'] = $dmids;
        }
        return $this->oauth->get('direct_messages/show_batch', $params);
    }

    /**
     * ????
     *
     * ????????????????????
     * <br />??API?{@link http://open.weibo.com/wiki/2/direct_messages/new direct_messages/new}
     * 
     * @access public
     * @param int $uid ??UID
     * @param string $text ?????????????????300????
     * @param int $id ???????ID?
     * @return array
     */
    function send_dm_by_id($uid, $text, $id = NULL) {
        $params = array();
        $this->id_format($uid);
        $params['text'] = $text;
        $params['uid'] = $uid;
        if ($id) {
            $this->id_format($id);
            $params['id'] = $id;
        }
        return $this->oauth->post('direct_messages/new', $params);
    }

    /**
     * ????
     *
     * ????????????????????
     * <br />??API?{@link http://open.weibo.com/wiki/2/direct_messages/new direct_messages/new}
     * 
     * @access public
     * @param string $screen_name ????
     * @param string $text ?????????????????300????
     * @param int $id ???????ID?
     * @return array
     */
    function send_dm_by_name($screen_name, $text, $id = NULL) {
        $params = array();
        $params['text'] = $text;
        $params['screen_name'] = $screen_name;
        if ($id) {
            $this->id_format($id);
            $params['id'] = $id;
        }
        return $this->oauth->post('direct_messages/new', $params);
    }

    /**
     * ??????
     *
     * ?ID???????????????????
     * <br />??API?{@link http://open.weibo.com/wiki/2/direct_messages/destroy direct_messages/destroy}
     * 
     * @access public
     * @param int $did ????????ID
     * @return array
     */
    function delete_dm($did) {
        $this->id_format($did);
        $params = array();
        $params['id'] = $did;
        return $this->oauth->post('direct_messages/destroy', $params);
    }

    /**
     * ??????
     *
     * ??????????????????????400???
     * <br />??API?{@link http://open.weibo.com/wiki/2/direct_messages/destroy_batch direct_messages/destroy_batch}
     * 
     * @access public
     * @param mixed $dids ????????ID????????????????ID????????20?????"4976494627, 4976262053"?array(4976494627,4976262053);
     * @return array
     */
    function delete_dms($dids) {
        $params = array();
        if (is_array($dids) && !empty($dids)) {
            foreach ($dids as $k => $v) {
                $this->id_format($dids[$k]);
            }
            $params['ids'] = join(',', $dids);
        } else {
            $params['ids'] = $dids;
        }

        return $this->oauth->post('direct_messages/destroy_batch', $params);
    }

    /**
     * ????????
     *
     * ??API?{@link http://open.weibo.com/wiki/2/account/profile/basic account/profile/basic}
     *
     * @param int $uid  ???????????UID???????????
     * @return array
     */
    function account_profile_basic($uid = NULL) {
        $params = array();
        if ($uid) {
            $this->id_format($uid);
            $params['uid'] = $uid;
        }
        return $this->oauth->get('account/profile/basic', $params);
    }

    /**
     * ?????????
     *
     * ??API?{@link http://open.weibo.com/wiki/2/account/profile/education account/profile/education}
     *
     * @param int $uid  ???????????UID???????????
     * @return array
     */
    function account_education($uid = NULL) {
        $params = array();
        if ($uid) {
            $this->id_format($uid);
            $params['uid'] = $uid;
        }
        return $this->oauth->get('account/profile/education', $params);
    }

    /**
     * ???????????
     *
     * ??API?{@link http://open.weibo.com/wiki/2/account/profile/education_batch account/profile/education_batch}
     *
     * @param string $uids ???????????UID??????????????20?
     * @return array
     */
    function account_education_batch($uids) {
        $params = array();
        if (is_array($uids) && !empty($uids)) {
            foreach ($uids as $k => $v) {
                $this->id_format($uids[$k]);
            }
            $params['uids'] = join(',', $uids);
        } else {
            $params['uids'] = $uids;
        }

        return $this->oauth->get('account/profile/education_batch', $params);
    }

    /**
     * ?????????
     *
     * ??API?{@link http://open.weibo.com/wiki/2/account/profile/career account/profile/career}
     *
     * @param int $uid  ???????????UID???????????
     * @return array
     */
    function account_career($uid = NULL) {
        $params = array();
        if ($uid) {
            $this->id_format($uid);
            $params['uid'] = $uid;
        }
        return $this->oauth->get('account/profile/career', $params);
    }

    /**
     * ???????????
     *
     * ??API?{@link http://open.weibo.com/wiki/2/account/profile/career_batch account/profile/career_batch}
     *
     * @param string $uids ???????????UID??????????????20?
     * @return array
     */
    function account_career_batch($uids) {
        $params = array();
        if (is_array($uids) && !empty($uids)) {
            foreach ($uids as $k => $v) {
                $this->id_format($uids[$k]);
            }
            $params['uids'] = join(',', $uids);
        } else {
            $params['uids'] = $uids;
        }

        return $this->oauth->get('account/profile/career_batch', $params);
    }

    /**
     * ??????????
     *
     * ??API?{@link http://open.weibo.com/wiki/2/account/get_privacy account/get_privacy}
     * 
     * @access public
     * @return array
     */
    function get_privacy() {
        return $this->oauth->get('account/get_privacy');
    }

    /**
     * ?????????
     *
     * ??API?{@link http://open.weibo.com/wiki/2/account/profile/school_list account/profile/school_list}
     *
     * @param array $query ????????array('key0'=>'value0', 'key1'=>'value1', ....)????key:
     *  - province	int		???????ID?
     *  - city		int		???????ID?
     *  - area		int		??????ID?
     *  - type		int		?????1????2????3??????4????5???????1?
     *  - capital	string	?????????A?
     *  - keyword	string	????????
     *  - count		int		???????????10?
     * ??keyword?capital??????????????????capital????????province???
     * @access public
     * @return array
     */
    function school_list($query) {
        $params = $query;

        return $this->oauth->get('account/profile/school_list', $params);
    }

    /**
     * ?????????API????????
     *
     * ??API?{@link http://open.weibo.com/wiki/2/account/rate_limit_status account/rate_limit_status}
     * 
     * @access public
     * @return array
     */
    function rate_limit_status() {
        return $this->oauth->get('account/rate_limit_status');
    }

    /**
     * OAuth????????????UID
     *
     * ??API?{@link http://open.weibo.com/wiki/2/account/get_uid account/get_uid}
     * 
     * @access public
     * @return array
     */
    function get_uid() {
        return $this->oauth->get('account/get_uid');
    }

    /**
     * ??????
     *
     * ??API?{@link http://open.weibo.com/wiki/2/account/profile/basic_update account/profile/basic_update}
     * 
     * @access public
     * @param array $profile ??????????array('key1'=>'value1', 'key2'=>'value2', .....)?
     * ???????
     *  - screen_name		string	??????????
     *  - gender	i		string	?????m???f????????
     *  - real_name			string	???????
     *  - real_name_visible	int		?????????0??????1???????2???????
     *  - province	true	int		????ID??????
     *  - city	true		int		????ID??????
     *  - birthday			string	????????yyyy-mm-dd?
     *  - birthday_visible	int		???????0????1???????2???????3???????
     *  - qq				string	??QQ???
     *  - qq_visible		int		??QQ?????0??????1???????2???????
     *  - msn				string	??MSN?
     *  - msn_visible		int		??MSN?????0??????1???????2???????
     *  - url				string	???????
     *  - url_visible		int		???????????0??????1???????2???????
     *  - credentials_type	int		?????1?????2?????3?????4????
     *  - credentials_num	string	?????
     *  - email				string	?????????
     *  - email_visible		int		?????????????0??????1???????2???????
     *  - lang				string	?????zh_cn??????zh_tw??????
     *  - description		string	??????????70????
     * ??birthday??????????
     *  - ????????1986-00-00???
     *  - ????????0000-08-00???
     *  - ????????0000-00-28???
     * @return array
     */
    function update_profile($profile) {
        return $this->oauth->post('account/profile/basic_update', $profile);
    }

    /**
     * ??????
     *
     * ??API?{@link http://open.weibo.com/wiki/2/account/profile/edu_update account/profile/edu_update}
     * 
     * @access public
     * @param array $edu_update ????????????array('key1'=>'value1', 'key2'=>'value2', .....)?
     * ???????
     *  - type			int		?????1????2????3??????4????5???????1?????
     *  - school_id	`	int		?????????
     *  - id			string	?????????ID??????????????
     *  - year			int		????????1900??????????
     *  - department	string	???????
     *  - visible		int		?????0???????1????????2???????
     * @return array
     */
    function edu_update($edu_update) {
        return $this->oauth->post('account/profile/edu_update', $edu_update);
    }

    /**
     * ????ID?????????
     *
     * ??API?{@link http://open.weibo.com/wiki/2/account/profile/edu_destroy account/profile/edu_destroy}
     * 
     * @param int $id ????????ID?
     * @return array
     */
    function edu_destroy($id) {
        $this->id_format($id);
        $params = array();
        $params['id'] = $id;
        return $this->oauth->post('account/profile/edu_destroy', $params);
    }

    /**
     * ??????
     *
     * ??API?{@link http://open.weibo.com/wiki/2/account/profile/car_update account/profile/car_update}
     * 
     * @param array $car_update ????????????array('key1'=>'value1', 'key2'=>'value2', .....)?
     * ???????
     *  - id			string	?????????ID?
     *  - start			int		??????????1900?????????
     *  - end			int		??????????0?
     *  - department	string	?????
     *  - visible		int		?????0??????1???????2???????
     *  - province		int		????ID???????
     *  - city			int		????ID???????
     *  - company		string	???????????
     * ??province?city??????<br />
     * ??id??????????????company????????id??????????company??
     * @return array
     */
    function car_update($car_update) {
        return $this->oauth->post('account/profile/car_update', $car_update);
    }

    /**
     * ????ID?????????
     *
     * ??API?{@link http://open.weibo.com/wiki/2/account/profile/car_destroy account/profile/car_destroy}
     * 
     * @access public
     * @param int $id  ????????ID
     * @return array
     */
    function car_destroy($id) {
        $this->id_format($id);
        $params = array();
        $params['id'] = $id;
        return $this->oauth->post('account/profile/car_destroy', $params);
    }

    /**
     * ????
     *
     * ??API?{@link http://open.weibo.com/wiki/2/account/avatar/upload account/avatar/upload}
     *
     * @param string $image_path ????????, ??url?[???png/jpg/gif????, ???????get_image_mime??] ?????700K????GIF, JPG??. ??????500?????????
     * @return array
     */
    function update_profile_image($image_path) {
        $params = array();
        $params['image'] = "@{$image_path}";

        return $this->oauth->post('account/avatar/upload', $params, true);
    }

    /**
     * ??????
     *
     * ??API?{@link http://open.weibo.com/wiki/2/account/update_privacy account/update_privacy}
     * 
     * @param array $privacy_settings ????????????array('key1'=>'value1', 'key2'=>'value2', .....)?
     * ???????
     *  - comment	int	???????????0?????1?????????0?
     *  - geo		int	?????????0?????1???????1?
     *  - message	int	??????????0?????1?????????0?
     *  - realname	int	?????????????0?????1???????0?
     *  - badge		int	???????0?????1???????1?
     *  - mobile	int	???????????????0?????1???????0?
     * ????????
     * @return array
     */
    function update_privacy($privacy_settings) {
        return $this->oauth->post('account/update_privacy', $privacy_settings);
    }

    /**
     * ???????????
     *
     * ??????????20??????????????????????
     * <br />??API?{@link http://open.weibo.com/wiki/2/favorites favorites}
     * 
     * @access public
     * @param  int $page ???????????1?
     * @param  int $count ?????????????50?
     * @return array
     */
    function get_favorites($page = 1, $count = 50) {
        $params = array();
        $params['page'] = intval($page);
        $params['count'] = intval($count);

        return $this->oauth->get('favorites', $params);
    }

    /**
     * ????ID?????????
     *
     * ????ID??????????
     * <br />??API?{@link http://open.weibo.com/wiki/2/favorites/show favorites/show}
     * 
     * @access public
     * @param int $id ???????ID?
     * @return array
     */
    function favorites_show($id) {
        $params = array();
        $this->id_format($id);
        $params['id'] = $id;
        return $this->oauth->get('favorites/show', $params);
    }

    /**
     * ?????????????????????
     *
     * ??API?{@link http://open.weibo.com/wiki/2/favorites/by_tags favorites/by_tags}
     *
     * 
     * @param int $tid  ???????ID?'
     * @param int $count ?????????????50?
     * @param int $page ???????????1?
     * @return array
     */
    function favorites_by_tags($tid, $page = 1, $count = 50) {
        $params = array();
        $params['tid'] = $tid;
        $params['count'] = $count;
        $params['page'] = $page;
        return $this->oauth->get('favorites/by_tags', $params);
    }

    /**
     * ???????????????
     *
     * ??API?{@link http://open.weibo.com/wiki/2/favorites/tags favorites/tags}
     * 
     * @access public
     * @param int $count ?????????????50?
     * @param int $page ???????????1?
     * @return array
     */
    function favorites_tags($page = 1, $count = 50) {
        $params = array();
        $params['count'] = $count;
        $params['page'] = $page;
        return $this->oauth->get('favorites/tags', $params);
    }

    /**
     * ????????
     *
     * ??API?{@link http://open.weibo.com/wiki/2/favorites/create favorites/create}
     * 
     * @access public
     * @param int $sid ?????id
     * @return array
     */
    function add_to_favorites($sid) {
        $this->id_format($sid);
        $params = array();
        $params['id'] = $sid;

        return $this->oauth->post('favorites/create', $params);
    }

    /**
     * ???????
     *
     * ??API?{@link http://open.weibo.com/wiki/2/favorites/destroy favorites/destroy}
     * 
     * @access public
     * @param int $id ??????????ID.
     * @return array
     */
    function remove_from_favorites($id) {
        $this->id_format($id);
        $params = array();
        $params['id'] = $id;
        return $this->oauth->post('favorites/destroy', $params);
    }

    /**
     * ?????????
     *
     * ??????????????????????HTTP400???
     * <br />??API?{@link http://open.weibo.com/wiki/2/favorites/destroy_batch favorites/destroy_batch}
     * 
     * @access public
     * @param mixed $fids ????????ID????????????????ID????????20?????"231101027525486630,201100826122315375"?array(231101027525486630,201100826122315375);
     * @return array
     */
    function remove_from_favorites_batch($fids) {
        $params = array();
        if (is_array($fids) && !empty($fids)) {
            foreach ($fids as $k => $v) {
                $this->id_format($fids[$k]);
            }
            $params['ids'] = join(',', $fids);
        } else {
            $params['ids'] = $fids;
        }

        return $this->oauth->post('favorites/destroy_batch', $params);
    }

    /**
     * ???????????
     *
     * ??API?{@link http://open.weibo.com/wiki/2/favorites/tags/update favorites/tags/update}
     * 
     * @access public
     * @param int $id ???????ID?
     * @param string $tags ???????????????????????2??
     * @return array
     */
    function favorites_tags_update($id, $tags) {
        $params = array();
        $params['id'] = $id;
        if (is_array($tags) && !empty($tags)) {
            foreach ($tags as $k => $v) {
                $this->id_format($tags[$k]);
            }
            $params['tags'] = join(',', $tags);
        } else {
            $params['tags'] = $tags;
        }
        return $this->oauth->post('favorites/tags/update', $params);
    }

    /**
     * ??????????????????
     *
     * ??API?{@link http://open.weibo.com/wiki/2/favorites/tags/update_batch favorites/tags/update_batch}
     *
     * @param int $tid  ???????ID???
     * @param string $tag  ????????????
     * @return array
     */
    function favorites_update_batch($tid, $tag) {
        $params = array();
        $params['tid'] = $tid;
        $params['tag'] = $tag;
        return $this->oauth->post('favorites/tags/update_batch', $params);
    }

    /**
     * ??????????????????
     *
     * ????????????????????????????????????
     * <br />??API?{@link http://open.weibo.com/wiki/2/favorites/tags/destroy_batch favorites/tags/destroy_batch}
     *
     * @param int $tid  ???????ID???
     * @return array
     */
    function favorites_tags_destroy_batch($tid) {
        $params = array();
        $params['tid'] = $tid;
        return $this->oauth->post('favorites/tags/destroy_batch', $params);
    }

    /**
     * ????????
     *
     * ??API?{@link http://open.weibo.com/wiki/2/trends trends}
     * 
     * @param int $uid ?????ID????????????
     * @param int $page ?????????????
     * @param int $count ????????10????
     * @return array
     */
    function get_trends($uid = NULL, $page = 1, $count = 10) {
        $params = array();
        if ($uid) {
            $params['uid'] = $uid;
        } else {
            $user_info = $this->get_uid();
            $params['uid'] = $user_info['uid'];
        }
        $this->id_format($params['uid']);
        $params['page'] = $page;
        $params['count'] = $count;
        return $this->oauth->get('trends', $params);
    }

    /**
     * ?????????????
     *
     * ??API?{@link http://open.weibo.com/wiki/2/trends/is_follow trends/is_follow}
     * 
     * @access public
     * @param string $trend_name ??????
     * @return array
     */
    function trends_is_follow($trend_name) {
        $params = array();
        $params['trend_name'] = $trend_name;
        return $this->oauth->get('trends/is_follow', $params);
    }

    /**
     * ?????????????
     *
     * ??API?{@link http://open.weibo.com/wiki/2/trends/hourly trends/hourly}
     * 
     * @param  int $base_app ??????????????1?????????????????0????
     * @return array
     */
    function hourly_trends($base_app = 0) {
        $params = array();
        $params['base_app'] = $base_app;

        return $this->oauth->get('trends/hourly', $params);
    }

    /**
     * ????????????
     *
     * ??API?{@link http://open.weibo.com/wiki/2/trends/daily trends/daily}
     * 
     * @param int $base_app ??????????????1?????????????????0????
     * @return array
     */
    function daily_trends($base_app = 0) {
        $params = array();
        $params['base_app'] = $base_app;

        return $this->oauth->get('trends/daily', $params);
    }

    /**
     * ????????????
     *
     * ??API?{@link http://open.weibo.com/wiki/2/trends/weekly trends/weekly}
     * 
     * @access public
     * @param int $base_app ??????????????1?????????????????0????
     * @return array
     */
    function weekly_trends($base_app = 0) {
        $params = array();
        $params['base_app'] = $base_app;

        return $this->oauth->get('trends/weekly', $params);
    }

    /**
     * ?????
     *
     * ??API?{@link http://open.weibo.com/wiki/2/trends/follow trends/follow}
     * 
     * @access public
     * @param string $trend_name ??????????
     * @return array
     */
    function follow_trends($trend_name) {
        $params = array();
        $params['trend_name'] = $trend_name;
        return $this->oauth->post('trends/follow', $params);
    }

    /**
     * ?????????
     *
     * ??API?{@link http://open.weibo.com/wiki/2/trends/destroy trends/destroy}
     * 
     * @access public
     * @param int $tid ????????ID?
     * @return array
     */
    function unfollow_trends($tid) {
        $this->id_format($tid);

        $params = array();
        $params['trend_id'] = $tid;

        return $this->oauth->post('trends/destroy', $params);
    }

    /**
     * ???????????
     *
     * ??API?{@link http://open.weibo.com/wiki/2/tags tags}
     * 
     * @param int $uid ?????ID????????????
     * @param int $page ?????????????
     * @param int $count ????????20????200????
     * @return array
     */
    function get_tags($uid = NULL, $page = 1, $count = 20) {
        $params = array();
        if ($uid) {
            $params['uid'] = $uid;
        } else {
            $user_info = $this->get_uid();
            $params['uid'] = $user_info['uid'];
        }
        $this->id_format($params['uid']);
        $params['page'] = $page;
        $params['count'] = $count;
        return $this->oauth->get('tags', $params);
    }

    /**
     * ???????????
     *
     * ??API?{@link http://open.weibo.com/wiki/2/tags/tags_batch tags/tags_batch}
     * 
     * @param  string $uids ????????ID???20????????
     * @return array
     */
    function get_tags_batch($uids) {
        $params = array();
        if (is_array($uids) && !empty($uids)) {
            foreach ($uids as $k => $v) {
                $this->id_format($uids[$k]);
            }
            $params['uids'] = join(',', $uids);
        } else {
            $params['uids'] = $uids;
        }
        return $this->oauth->get('tags/tags_batch', $params);
    }

    /**
     * ??????????
     *
     * ??API?{@link http://open.weibo.com/wiki/2/tags/suggestions tags/suggestions}
     * 
     * @access public
     * @param int $count ????????10????10????
     * @return array
     */
    function get_suggest_tags($count = 10) {
        $params = array();
        $params['count'] = intval($count);
        return $this->oauth->get('tags/suggestions', $params);
    }

    /**
     * ???????????????
     *
     * ??API?{@link http://open.weibo.com/wiki/2/tags/create tags/create}
     * 
     * @access public
     * @param mixed $tags ????????????????????7????14????????????????????????????????"abc,drf,efgh,tt"?array("abc", "drf", "efgh", "tt")
     * @return array
     */
    function add_tags($tags) {
        $params = array();
        if (is_array($tags) && !empty($tags)) {
            $params['tags'] = join(',', $tags);
        } else {
            $params['tags'] = $tags;
        }
        return $this->oauth->post('tags/create', $params);
    }

    /**
     * ????
     *
     * ??API?{@link http://open.weibo.com/wiki/2/tags/destroy tags/destroy}
     * 
     * @access public
     * @param int $tag_id ??ID?????
     * @return array
     */
    function delete_tag($tag_id) {
        $params = array();
        $params['tag_id'] = $tag_id;
        return $this->oauth->post('tags/destroy', $params);
    }

    /**
     * ??????
     *
     * ??API?{@link http://open.weibo.com/wiki/2/tags/destroy_batch tags/destroy_batch}
     * 
     * @access public
     * @param mixed $ids ?????????tag id???id??????????10??????tag id????????“553,554,555"?array(553, 554, 555)
     * @return array
     */
    function delete_tags($ids) {
        $params = array();
        if (is_array($ids) && !empty($ids)) {
            $params['ids'] = join(',', $ids);
        } else {
            $params['ids'] = $ids;
        }
        return $this->oauth->post('tags/destroy_batch', $params);
    }

    /**
     * ????????????????
     *
     * ??API?{@link http://open.weibo.com/wiki/2/register/verify_nickname register/verify_nickname}
     *
     * @param string $nickname ????????4-20?????????????"_"??????
     * @return array
     */
    function verify_nickname($nickname) {
        $params = array();
        $params['nickname'] = $nickname;
        return $this->oauth->get('register/verify_nickname', $params);
    }

    /**
     * ????????????
     *
     * ??API?{@link http://open.weibo.com/wiki/2/search/suggestions/users search/suggestions/users}
     *
     * @param string $q ??????????URLencoding???,??????????
     * @param int $count ???????????10?
     * @return array
     */
    function search_users($q, $count = 10) {
        $params = array();
        $params['q'] = $q;
        $params['count'] = $count;
        return $this->oauth->get('search/suggestions/users', $params);
    }

    /**
     * ????????????
     *
     * ??API?{@link http://open.weibo.com/wiki/2/search/suggestions/statuses search/suggestions/statuses}
     *
     * @param string $q ??????????URLencoding???
     * @param int $count ???????????10?
     * @return array
     */
    function search_statuses($q, $count = 10) {
        $params = array();
        $params['q'] = $q;
        $params['count'] = $count;
        return $this->oauth->get('search/suggestions/statuses', $params);
    }

    /**
     * ????????????
     *
     * ??API?{@link http://open.weibo.com/wiki/2/search/suggestions/schools search/suggestions/schools}
     *
     * @param string $q ??????????URLencoding???
     * @param int $count ???????????10?
     * @param int type ?????0????1????2????3??????4????5???????0???
     * @return array
     */
    function search_schools($q, $count = 10, $type = 1) {
        $params = array();
        $params['q'] = $q;
        $params['count'] = $count;
        $params['type'] = $type;
        return $this->oauth->get('search/suggestions/schools', $params);
    }

    /**
     * ????????????
     *
     * ??API?{@link http://open.weibo.com/wiki/2/search/suggestions/companies search/suggestions/companies}
     *
     * @param string $q ??????????URLencoding???
     * @param int $count ???????????10?
     * @return array
     */
    function search_companies($q, $count = 10) {
        $params = array();
        $params['q'] = $q;
        $params['count'] = $count;
        return $this->oauth->get('search/suggestions/companies', $params);
    }

    /**
     * ?????????
     *
     * ??API?{@link http://open.weibo.com/wiki/2/search/suggestions/at_users search/suggestions/at_users}
     *
     * @param string $q ??????????URLencoding???
     * @param int $count ???????????10?
     * @param int $type ?????0????1??????
     * @param int $range ?????0????????1???????????2???????2???
     * @return array
     */
    function search_at_users($q, $count = 10, $type = 0, $range = 2) {
        $params = array();
        $params['q'] = $q;
        $params['count'] = $count;
        $params['type'] = $type;
        $params['range'] = $range;
        return $this->oauth->get('search/suggestions/at_users', $params);
    }

    /**
     * ???????????????????
     *
     * ??API?{@link http://open.weibo.com/wiki/2/search/statuses search/statuses}
     *
     * @param array $query ????????array('key0'=>'value0', 'key1'=>'value1', ....)????key:
     *  - q				string	???????????URLencode?
     *  - filter_ori	int		??????????0????1????2???????0?
     *  - filter_pic	int		???????????0????1????2????????0?
     *  - fuid			int		??????????UID?
     *  - province		int		??????????ID?
     *  - city			int		??????????ID?
     *  - starttime		int		?????Unix????
     *  - endtime		int		?????Unix????
     *  - count			int		?????????????10?
     *  - page			int		???????????1?
     *  - needcount		boolean	???????????????true????false????????false?
     *  - base_app		int		?????????????0?????????1?????????????0?
     * needcount??????????????????
     * ????????
     * @return array
     */
    function search_statuses_high($query) {
        return $this->oauth->get('search/statuses', $query);
    }

    /**
     * ?????????
     *
     * ??API?{@link http://open.weibo.com/wiki/2/search/users search/users}
     *
     * @param array $query ????????array('key0'=>'value0', 'key1'=>'value1', ....)????key:
     *  - q			string	???????????URLencode?
     *  - snick		int		???????????0?????1????
     *  - sdomain	int		?????????????0?????1????
     *  - sintro	int		???????????0?????1????
     *  - stag		int		???????????0?????1????
     *  - province	int		??????????ID?
     *  - city		int		??????????ID?
     *  - gender	string	????????m???f???
     *  - comorsch	string	??????????
     *  - sort		int		?????1???????2?????????1?
     *  - count		int		?????????????10?
     *  - page		int		???????????1?
     *  - base_app	int		?????????????0?????????1?????????????0?
     * ??????????
     * @return array
     */
    function search_users_keywords($query) {
        return $this->oauth->get('search/users', $query);
    }

    /**
     * ????????
     *
     * ????????????
     * <br />??API?{@link http://open.weibo.com/wiki/2/suggestions/users/hot suggestions/users/hot}
     * 
     * @access public
     * @param string $category ??????????????????????? default?????????????????<br />
     *  - default:????
     *  - ent:????
     *  - hk_famous:????
     *  - model:??
     *  - cooking:??&??
     *  - sport:????
     *  - finance:????
     *  - tech:IT???
     *  - singer:??
     *  - writer???
     *  - moderator:???
     *  - medium:????
     *  - stockplayer:????
     * @return array
     */
    function hot_users($category = "default") {
        $params = array();
        $params['category'] = $category;

        return $this->oauth->get('suggestions/users/hot', $params);
    }

    /**
     * ???????????
     *
     * ??API?{@link http://open.weibo.com/wiki/2/suggestions/users/may_interested suggestions/users/may_interested}
     * 
     * @access public
     * @param int $page ???????????1?
     * @param int $count ?????????????10?
     * @return array
     * @ignore
     */
    function suggestions_may_interested($page = 1, $count = 10) {
        $params = array();
        $params['page'] = $page;
        $params['count'] = $count;
        return $this->oauth->get('suggestions/users/may_interested', $params);
    }

    /**
     * ????????????????? 
     *
     * ??API?{@link http://open.weibo.com/wiki/2/suggestions/users/by_status suggestions/users/by_status}
     * 
     * @access public
     * @param string $content ???????
     * @param int $num ??????????10?
     * @return array
     */
    function suggestions_users_by_status($content, $num = 10) {
        $params = array();
        $params['content'] = $content;
        $params['num'] = $num;
        return $this->oauth->get('suggestions/users/by_status', $params);
    }

    /**
     * ????
     *
     * ??API?{@link http://open.weibo.com/wiki/2/suggestions/favorites/hot suggestions/favorites/hot}
     *
     * @param int $count ??????????20???
     * @param int $page ???????1???
     * @return array
     */
    function hot_favorites($page = 1, $count = 20) {
        $params = array();
        $params['count'] = $count;
        $params['page'] = $page;
        return $this->oauth->get('suggestions/favorites/hot', $params);
    }

    /**
     * ????????????
     *
     * ??API?{@link http://open.weibo.com/wiki/2/suggestions/users/not_interested suggestions/users/not_interested}
     *
     * @param int $uid ????????UID?
     * @return array
     */
    function put_users_not_interested($uid) {
        $params = array();
        $params['uid'] = $uid;
        return $this->oauth->post('suggestions/users/not_interested', $params);
    }

    // =========================================

    /**
     * @ignore
     */
    protected function request_with_pager($url, $page = false, $count = false, $params = array()) {
        if ($page)
            $params['page'] = $page;
        if ($count)
            $params['count'] = $count;

        return $this->oauth->get($url, $params);
    }

    /**
     * @ignore
     */
    protected function request_with_uid($url, $uid_or_name, $page = false, $count = false, $cursor = false, $post = false, $params = array()) {
        if ($page)
            $params['page'] = $page;
        if ($count)
            $params['count'] = $count;
        if ($cursor)
            $params['cursor'] = $cursor;

        if ($post)
            $method = 'post';
        else
            $method = 'get';

        if ($uid_or_name !== NULL) {
            $this->id_format($uid_or_name);
            $params['id'] = $uid_or_name;
        }

        return $this->oauth->$method($url, $params);
    }

    /**
     * @ignore
     */
    protected function id_format(&$id) {
        if (is_float($id)) {
            $id = number_format($id, 0, '', '');
        } elseif (is_string($id)) {
            $id = trim($id);
        }
    }

    function create_live_stream($title, $width, $height, $summary = NULL, $published = 0, $image = NULL, $replay = 0, $is_panolive = 0) {
        $params = array();
        $params['title'] = $title;
        $params['width'] = $width;
        $params['height'] = $height;
        if ($summary) {
            $params['summary'] = $summary;
        }

        $params['published'] = $published;
        if ($image) {
            $params['image'] = $image;
        }

        $params['replay'] = $replay;
        $params['is_panolive'] = $is_panolive;

        return $this->oauth->post('proxy/live/create', $params);
    }

}
